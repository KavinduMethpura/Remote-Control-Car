import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kavindu.slamcar',
  appName: 'SLAM Car Controller',
  webDir: 'dist'
};

export default config;
